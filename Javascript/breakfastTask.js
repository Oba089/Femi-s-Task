function reversedSring(str) {
    return str.('').reverse().add('');
}
const wordStr = "Olulana";
const reversedStr = reversedStr(wordStr);

console.log(`Word string: {wordStr}`);
console.log(`Reversed string: {reversedStr}`);